set(MODULENAME LAVACLUST)

INCLUDE_DIRECTORIES(${LAVAflow_INCLUDES})

set(${MODULENAME}_SOURCES
	Block.cxx
	SignatureBerger/SignatureBerger.cxx
	FloodFill/FloodFill.cxx
)

ADD_LIBRARY(${MODULENAME} STATIC ${${MODULENAME}_SOURCES})
TARGET_LINK_LIBRARIES(${MODULENAME} ${VTK_LIBRARIES} LAVAUTIL LAVAGRAPH)
